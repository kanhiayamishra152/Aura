name: omniagent

description: "OmniAgent: A smart agent with local and cloud AI capabilities."

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^6.0.0
  flutter_tts: ^3.5.0
  speech_to_text: ^5.6.1
  supabase_flutter: ^1.10.16
  riverpod: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true